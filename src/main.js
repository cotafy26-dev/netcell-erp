// Ponto de entrada do bundle (Vite).
// A interação da página (menu, scroll reveal, links de WhatsApp) está no
// <script> inline do index.html, que não depende de módulos.
// Este arquivo existe para o projeto ter a estrutura padrão de app Vite.

document.documentElement.dataset.build = 'vite'
console.info('Netcell Informática — site carregado')
